include ./mixins.jade

extends layout

block content
  +header
    +headerCreateButton

  .markdown-body
    h1
      a(href="https://github.com/#{user}") #{user}
      | /
      a(href="https://github.com/#{user}/#{repo}") #{repo}
    !{content}
